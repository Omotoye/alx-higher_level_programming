#!/usr/bin/python3


def pow(a: int, b: int) -> int:
    """A function that computes a to the power
    of b

    Args:
        a (int): Number to be raised to a power
        b (int): Power to raise number to

    Returns:
        int: result from the raise to power operation
    """
    return a**b
